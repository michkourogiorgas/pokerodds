const Modal = () => {
  return (
    <dialog open>
      <p>Calculations may take up to 40 seconds.</p>
      <p>Currently processing 8,329,384 calculations.</p>
      <p>
        To speed up the process, you can provide some flop cards as an
        alternative.
      </p>
    </dialog>
  );
};

export default Modal;
